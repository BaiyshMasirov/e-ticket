name: authentication
description: Authentication Module
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.1.1 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  common:
    path: ../common

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  build_runner:
  freezed: ^2.4.5
  json_serializable: ^6.7.0

flutter: